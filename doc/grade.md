# Ожидаемые баллы

## Задание 1 (макс. 57 баллов)

### 1. Сбор данных (макс. 10 баллов)

| Пункт | Статус | Баллы | Пояснение |
|-------|--------|-------|-----------|
| Разделение на батчи и эмуляция потока | Готово | 1 | `ingestion.py`: датасет нарезается на 10 батчей по 10 000 строк через `iloc`, `state.json` хранит указатель на следующий батч |
| Хранилище сырых данных: файловая система | Готово | 1 | Каждый батч сохраняется в `artifacts/data/raw/batch_NNN.csv` |
| Расчёт метапараметров | Готово | 2 | Для каждого батча вычисляются: кол-во строк/колонок, % пропусков, диапазон дат, распределение таргета — сохраняются в `meta_batch_NNN.json` |
| Конфигурационный файл (YAML) | Готово | 1 | `configs/data.yaml`, `training.yaml`, `analysis.yaml`, `serving.yaml` — все параметры системы вынесены в конфиги |
| Система логирования и обработки ошибок | Готово | 2 | `logging` с выводом в файл и stdout, `try/except` с логированием исключений в `run_update()` |
| Интеграция с несколькими источниками данных | Не реализовано | 0 | Используется один источник (Kaggle CSV) |

### 2. Анализ данных (макс. 10 баллов)

| Пункт | Статус | Баллы | Пояснение |
|-------|--------|-------|-----------|
| Оценка и хранение показателей Data Quality | Готово | 2 | `analysis.py`: % пропусков по колонкам, % дублей, выбросы по z-score — сохраняются в `dq_batch_NNN.json` |
| Базовая очистка данных по порогам | Готово | 1 | Колонки с пропусками выше порога удаляются, строки-дубликаты удаляются |
| Автоматический EDA | Готово | 2 | Описательные статистики числовых колонок, топ-5 категориальных значений, корреляции Пирсона с таргетом — `eda_batch_NNN.json` |
| Feature Engineering | Готово | 1 | OLS-регрессия через statsmodels: p-value и VIF для каждого признака, незначимые исключаются |
| Генерация отчётов о качестве данных | Готово | 1 | Отчёт формируется командой `python run.py -mode summary` |
| Мониторинг Data Drift (KS-тест) | Готово | 1 | KS-тест между текущим и предыдущим батчем, предупреждение в лог при p < 0.05 |

### 3. Подготовка данных (макс. 5 баллов)

| Пункт | Статус | Баллы | Пояснение |
|-------|--------|-------|-----------|
| Обработка пропусков | Готово | 1 | Медиана для числовых, "Unknown" для категориальных |
| Обработка категориальных переменных | Готово | 1 | CatBoost работает с категориальными нативно, список `cat_features` передаётся в Pool |
| Обработка числовых переменных | Готово | 1 | Удаление выбросов, нормировка не нужна (CatBoost инвариантен к масштабу) |
| Несколько вариантов предобработки | Не реализовано | 0 | Используется единственный pipeline подготовки |

### 4. Обучение/дообучение модели (макс. 5 баллов)

| Пункт | Статус | Баллы | Пояснение |
|-------|--------|-------|-----------|
| Построение модели (CatBoostRegressor) | Готово | 1 | Модель обучается на каждом батче |
| Инкрементальное дообучение (init_model) | Готово | 2 | `model.fit(pool, init_model=prev)` — каждый батч добавляет 100 деревьев поверх предыдущей модели без обучения с нуля |
| Несколько моделей с разной устойчивостью | Не реализовано | 0 | Реализована только одна модель (CatBoost) |

### 5. Валидация модели (макс. 10 баллов)

| Пункт | Статус | Баллы | Пояснение |
|-------|--------|-------|-----------|
| Оценка качества (hold-out): RMSE, MAE, R2 | Готово | 2 | 20% накопленных данных откладывается как тестовая выборка |
| Хранилище версий моделей (registry.json) | Готово | 2 | Каждая версия модели с метриками сохраняется в `registry.json`; реализован промоушн при улучшении RMSE на >= 0.01 |
| Мониторинг Model Drift | Готово | 1 | Если последняя RMSE хуже исторического минимума на > 10% — warning в лог |
| Интерпретация прогнозов (LIME, SHAP и др.) | Не реализовано | 0 | — |

### 6. Обслуживание модели (макс. 6 баллов)

| Пункт | Статус | Баллы | Пояснение |
|-------|--------|-------|-----------|
| Выбор и сериализация production модели | Готово | 2 | `serving.py`: модель + препарер сериализуются в единый pickle-бандл `latest.pkl` |
| Мониторинг производительности | Готово | 2 | Каждый вызов inference замеряет latency (мс) и пик памяти (КБ) через `tracemalloc`, результат пишется в `monitoring.json` |
| Гибкий прогноз при аномальных данных | Не реализовано | 0 | Модель применяется без специальной обработки аномальных входных данных |

### 7. Управление программой (макс. 11 баллов)

| Пункт | Статус | Баллы | Пояснение |
|-------|--------|-------|-----------|
| Скрипт управления (inference / update / summary) | Готово | 2 | `run.py` с argparse обрабатывает все три режима |
| README и requirements | Готово | 1 | README с описанием системы, структуры, результатов; `requirements.txt` |
| Расширенный отчёт | Готово | 1 | `summary` генерирует Markdown-отчёт с таблицами DQ, метриками моделей, историей inference |
| Конфигурационный файл системы | Готово | 2 | 4 YAML-файла покрывают все параметры: размер батча, пороги качества, гиперпараметры, пути |
| Meta Learning модель | Не реализовано | 0 | — |
| Архитектура на основе паттернов (MVC и др.) | Не реализовано | 0 | Используется линейная структура модулей без явного паттерна |

---

## Задание 2 (макс. 16 баллов)

### 1. Функциональность workflow (макс. 8 баллов)

| Пункт | Статус | Баллы | Пояснение |
|-------|--------|-------|-----------|
| Автозапуск при push / pull request | Готово | 1 | `on: push / pull_request` в `ci_cd.yml` |
| Корректная установка окружения | Готово | 1 | `pip install -r requirements.txt` с кэшированием через `actions/setup-python` |
| Успешное обучение модели | Готово | 1 | Job `train` запускает `python run.py -mode update` |
| Управление итерациями из YAML | Готово | 1 | `iterations_per_batch` читается из `configs/training.yaml` |
| CRON-инкрементальное дообучение | Готово | 3 | Расписание `0 * * * *` (каждый час); модель и `state.json` кэшируются между запусками через `actions/cache` |
| Тесты ML-системы через CI/CD | Готово | 1 | Job `test` запускает `pytest tests/` перед обучением; 7 тестов покрывают конфиги, препарер, тренер и валидатор |
| Иное расширение (параллельные jobs, БД и др.) | Не реализовано | 0 | — |

### 2. Управление артефактами (макс. 6 баллов)

| Пункт | Статус | Баллы | Пояснение |
|-------|--------|-------|-----------|
| Сохранение логов обучения | Готово | 1 | `artifacts/logs/` загружается как `training-logs-N` через `actions/upload-artifact` |
| Сохранение сериализованной модели | Готово | 1 | `artifacts/models/production/` загружается как `production-model-N` |
| Сохранение препарера | Готово | 1 | Препарер сериализован вместе с моделью в `latest.pkl` |
| Dashboard истории обучения / онлайн-сервис | Не реализовано | 0 | — |

### 3. Документирование (макс. 2 балла)

| Пункт | Статус | Баллы | Пояснение |
|-------|--------|-------|-----------|
| Инструкции по развёртыванию в README | Готово | 1 | Раздел CI/CD в README: настройка секретов, ручной запуск, список артефактов |
| Описание и аргументация подхода | Готово | 1 | Обоснование выбора CRON-инкрементального сценария в README и `doc/task.md` |
